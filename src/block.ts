import { ID, Container } from "./exports";

/** A Block is a record-like item containing a fixed set of items ot different types and optionally named */

export class Block extends Container {


  
}

/** document-unique ID of a block item */
export class BlockID extends ID {

}
