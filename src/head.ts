import { Block, Record } from "./exports";

/** A Head is the top program-visible value of a workspace. Heads are contained
 * in the History block. The first field of the head imports the builtins */
export class Head extends Record {


}
